{{- if .Values.collector.enabled }}
apiVersion: v1
kind: Secret
metadata:
  name: incident-collector-secret
  namespace: {{ .Values.namespace }}
type: Opaque
stringData:
  webhook-token: {{ .Values.collector.webhookToken | quote }}
{{- end }}